import React from 'react';

import Loading from '.';

export const DefaultLoading = () => {
  return (
    <div>
      <Loading />
    </div>
  );
};

const story = {
  title: 'Components/Loading',
  component: Loading,
};

export default story;
